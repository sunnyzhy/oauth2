<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.springframework.security.oauth.mapper.OauthExtendAuthorityMapper">
  <resultMap id="BaseResultMap" type="org.springframework.security.oauth.entity.OauthExtendAuthority">
    <!--
      WARNING - @mbg.generated
    -->
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="role" jdbcType="VARCHAR" property="role" />
  </resultMap>

  <select id="selectRole" resultMap="BaseResultMap">
    SELECT * FROM oauth_extend_authority a
    INNER JOIN oauth_extend_user_authority ua ON a.id = ua.authority_id
    INNER JOIN oauth_extend_user_details ud ON ua.user_id = ud.id
    WHERE ud.id = #{userId};
  </select>
</mapper>